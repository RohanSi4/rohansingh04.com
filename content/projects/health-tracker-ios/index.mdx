# health tracker ios app

started building this as a standalone ios app. the idea evolved into the live
health dashboard on the landing page of this site, which uses an ios shortcut
+ webhook pipeline instead of a full app.
